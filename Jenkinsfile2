pipeline {
  agent {
    kubernetes {
      label 'jenkins-slave'
      defaultContainer 'jnlp'
      tools {nodejs "node"}
      yaml """
apiVersion: v1
kind: Pod
spec:
  containers:
  - name: docker
    env:
    - name: DOCKER_HOST
      value: 127.0.0.1
    image: docker
    command:
    - cat
    tty: true
  
"""
    }
  }
  stages {

    stage('Image Build') {
      environment {
        DOCKERHUB_CREDS = credentials('demo-docker')
      }
      steps {
        container('docker') {
          sh "npm install"
        }
      }
    }
   }
   }
